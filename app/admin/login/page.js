'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const [error,setError]=useState('');const router=useRouter();async function submit(e){e.preventDefault();const form=new FormData(e.currentTarget);const r=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});if(r.ok){router.push('/admin/dashboard');router.refresh()}else setError('Invalid username or password');}return <main className="login-page"><form className="login-card" onSubmit={submit}><h1>Admin Login</h1><label>Username</label><input name="username" defaultValue="admin" required/><label>Password</label><input name="password" type="password" defaultValue="admin123" required/>{error&&<p style={{color:'crimson'}}>{error}</p>}<button className="primary-button" type="submit">Login</button><p style={{marginTop:14,fontSize:13}}>Local default: admin / admin123</p></form></main>}
