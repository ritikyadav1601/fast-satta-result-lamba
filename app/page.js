import SiteChrome from '@/components/SiteChrome';
import { Hero, ResultsAndCharts } from '@/components/HomeSections';
import HomepageSeoContent from '@/components/HomepageSeoContent';
import { getHomepageData } from '@/lib/store';
import { getExtraGamesData } from '@/lib/extra-games';
import ExtraGamesResults from '@/components/ExtraGamesResults';
import {getSplitMainGames} from '@/lib/main-games';

export const dynamic = 'force-dynamic';
const dynamicDate=()=>new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'long',year:'numeric',timeZone:'Asia/Kolkata'}).format(new Date());
export function generateMetadata(){const date=dynamicDate();return {title:`Satta Result Today ${date} | Fast Satta Result Live`,description:`Get the latest Fast Satta Result Today (${date}) live. Access real-time Satta Result Live records, and track historical Satta King Chart data instantly.`,keywords:['Satta Result Today','Fast Satta Result','Satta Result Live','Satta King Result','Satta King Chart','Gali Result','Desawar Result','Faridabad Result','Ghaziabad Result'],authors:[{name:'Fast Satta Result'}],creator:'Fast Satta Result',publisher:'Fast Satta Result',robots:{index:true,follow:true,googleBot:{index:true,follow:true}},alternates:{canonical:'https://fast-satta-result.com/'}}}
const normalize=value=>String(value||'').trim().toLowerCase().replace(/[^a-z]+/g,' ').trim().replace('desawer','desawar');
export default async function Home() { const [data,extra,split]=await Promise.all([getHomepageData(),getExtraGamesData(),getSplitMainGames()]);const legacyUpper=data.games.filter(game=>game.status!==false&&normalize(game.englishName||game.english_name)!=='disawar');const newMainByName=new Map(split.games.map(game=>[normalize(game.name),game]));const results=[];const games=legacyUpper.map(legacy=>{const key=normalize(legacy.englishName||legacy.english_name),fresh=newMainByName.get(key.replace('shree ganesh','shri ganesh').replace('gaziabad','ghaziabad'));if(!fresh){results.push(...data.results.filter(row=>String(row.gameId)===String(legacy.id)));return {...legacy,source:'legacy'}}results.push(...split.results.filter(row=>String(row.gameId)===String(fresh.id)));return {...fresh,name:legacy.name,englishName:key,resultTime:legacy.resultTime||legacy.result_time,time:legacy.time,source:'main'}});const desawar=split.games.find(game=>normalize(game.name)==='desawar')||data.games.find(game=>normalize(game.englishName||game.english_name)==='disawar');if(desawar){games.push({...desawar,source:desawar.source||'legacy'});const desawarResults=desawar.source==='main'?split.results:data.results;results.push(...desawarResults.filter(row=>String(row.gameId)===String(desawar.id)))}const mainData={...data,games,results};const extraData={games:[...split.otherGames,...extra.games],results:[...split.otherResults,...extra.results]};return <SiteChrome active="home" settings={data.settings}><Hero {...mainData}/><ResultsAndCharts {...mainData}/><ExtraGamesResults {...extraData}/><HomepageSeoContent date={dynamicDate()}/></SiteChrome> }
